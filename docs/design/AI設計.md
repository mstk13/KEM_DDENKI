# AI設計 — KEM_DDENKI における AI/ML の全体像

> このドキュメントは AI に関する設計判断の要約と道案内です。
> 各判断の詳細な根拠・実測データ・代替案の検討は、リンク先の ADR を参照してください。

---

## 目次

1. [設計思想](#設計思想)
2. [3層アーキテクチャ](#3層アーキテクチャ)
3. [各層の詳細](#各層の詳細)
4. [利用箇所マップ](#利用箇所マップ)
5. [コスト管理](#コスト管理)
6. [データフロー](#データフロー)
7. [ADR 一覧](#adr-一覧)

---

## 設計思想

KEM_DDENKI の AI は **3つの原則** で設計されている。

```
1. 機能は一つも削らない。品質が価値の中核でないタスクだけ実行先を変える
2. 決まった形のものは決まった手順で読み、崩れたものだけモデルに回す
3. 分からないことを分かったように書かない
```

AI は「人間の判断を代替する」のではなく、「人間が判断するための材料を速く揃える」ために使う。
自動確定できるものは確定し、曖昧なものは候補を出して人に選ばせる。
判定できないものを「資格なし」と断定せず「要確認」と返す。

---

## 3層アーキテクチャ

AI 推論をコスト・速度・品質で3層に分け、タスク種別で振り分ける。
詳細: [ADR-0010 AI推論の3層分割](../saas/adr/ADR-0010-ai-inference-tiering.md)

```
                        ┌─────────────────────────────┐
                        │     Django (web コンテナ)     │
                        └──────────┬──────────────────┘
                                   │
                    ┌──────────────┼──────────────────┐
                    ▼              ▼                   ▼
            ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
            │   層A        │ │   層B        │ │   層C        │
            │  Embedding   │ │  ローカルLLM  │ │  Claude API  │
            │              │ │              │ │              │
            │ bge-m3       │ │ Qwen3-8B     │ │ Haiku/Sonnet │
            │ 1,024次元     │ │ Q4_K_M 5GB   │ │ 従量課金      │
            │              │ │              │ │              │
            │ ≒0円/回      │ │ ≒0.04円/回   │ │ ≒1.2円/回    │
            │ <1秒         │ │ 30秒         │ │ 13秒         │
            └──────────────┘ └──────────────┘ └──────────────┘
                 │                  │                  │
                 ▼                  ▼                  ▼
            品目名の         PDF構造化         分析・提案
            類似度マッチング    納品書OCR         コスト最適化
                              取引先抽出         工程提案
```

### 振り分けの基準

**「安いから移す」ではなく「遅くても困らないから移す」。**

| 基準 | 層A/B（ローカル） | 層C（API） |
|------|------------------|-----------|
| 応答速度の要件 | バッチ処理。遅くても業務に影響なし | 画面から同期呼び出し。待ち時間がUXに直結 |
| 品質の重要度 | 候補生成。最終判断は人または層Cが行う | 建設業の文脈判断そのものが機能の価値 |
| 呼び出し頻度 | 数百〜数千回/月 | 数十回/月 |

---

## 各層の詳細

### 層A — Embedding（ベクトル類似度）

**目的:** 積算品目の名寄せ（同じ材料の表記ゆれを同一品目にマッチング）

**技術:** bge-m3 モデル（1,024次元ベクトル）

**実装:** `apps/estimation/services/matching.py`

**判定の3段構え（実測に基づく閾値）:**

```
コサイン類似度
  0.85以上  →  自動確定
  0.65〜0.85 →  上位k件を層C(LLM)に渡して選ばせる
  0.65未満  →  不一致
```

**なぜ単一閾値ではダメか（実測データ）:**

| 対象 | 類似度 | 判定 |
|------|-----:|------|
| VVFケーブル1.6-2C 100m巻き（表記ゆれ） | 0.904 | 同一 |
| VVF 1.6×2C 100m（略記） | 0.768 | 曖昧 |
| CVケーブル 2.0mm 3芯（**別種**） | 0.668 | 曖昧 |
| エアコン用コンセント | 0.443 | 別物 |

同一品目の略記（0.768）と別種ケーブル（0.668）の差はわずか0.10。
単一閾値で自動確定するとCVケーブルをVVFとして誤マッチする。

詳細: [ADR-0010 §導入後の実測](../saas/adr/ADR-0010-ai-inference-tiering.md#導入後の実測2026-08-14)

---

### 層B — ローカルLLM（Qwen3-8B）

**目的:** PDF構造化、納品書OCR、取引先名抽出

**技術:** Qwen3-8B Q4_K_M（Ollama、Windows ネイティブ）

**実装:** `apps/ai/services/llm_advisor.py`（プロバイダ抽象）

**なぜ 8B で 14B ではないか（VRAM制約）:**

```
RTX 3060 12GB の実効 VRAM ≒ 11GB

Qwen3-8B Q4 + 埋め込み = 6.7GB  → 空き 4.3GB (余裕あり)
Qwen3-14B Q4           = 9.2GB  → 空き 0.6GB (KVキャッシュが足りない)
```

**性能実測:**
- 生成速度: 58 tok/s（想定45〜55を上回る）
- 推論時消費電力: 155W / 74℃
- JSON Schema 指定で出力トークンが 1,550 → 111 に削減

**フォールバック:** ローカル失敗時は Claude API に自動切り替え。機能を止めるのではなくコストを払って動かす。

詳細: [ADR-0010 AI推論の3層分割](../saas/adr/ADR-0010-ai-inference-tiering.md)

---

### 層C — Claude API

**目的:** コスト最適化提案、工程提案、工程リスク分析、文字化けPDF解読

**技術:** Claude API（Haiku: 抽出系 / Sonnet: 分析・判断系）

**実装:** `apps/ai/services/llm_advisor.py`

**モデル使い分け:**

| モデル | 用途 | コスト/回 | 使い分けの理由 |
|--------|------|----------|--------------|
| Haiku | 抽出・分類 | ¥0.18 | 定型的な情報抽出。速度とコスト優先 |
| Sonnet | 分析・提案・PDF解読 | ¥1.2 | 建設業の文脈判断・参加資格の解釈。品質が機能の価値 |

**文字化けPDFの処理（層Cでしかできない仕事）:**

フォントに ToUnicode が無い PDF は pdfplumber が `(cid:NNN)` しか返せない。
Claude は PDF をページ画像として読めるため、OCR ライブラリ（tesseract）を追加せずに解決できる。

```
決定論的に読めた → そのまま使う（LLM不要）
読めない + (cid)検出 → Claude Sonnet に画像として渡す
```

65件中1件が該当。tesseract を Dockerfile に入れるリスクより、¥10/件の API 費用の方が安全。

詳細: [ADR-0006 Claude API統合](../saas/adr/ADR-0006-claude-api-integration.md) / [ADR-0012 文字化けPDF](../saas/adr/ADR-0012-garbled-announcement-llm-fallback.md)

---

## 利用箇所マップ

AI が使われている全箇所と、どの層で処理されるかの一覧。

```
┌─ 入札管理 (bids) ──────────────────────────────────────────────┐
│                                                                 │
│  i-ppiスクレイピング ──→ 公告PDF取得 ──→ 要件抽出              │
│                              │              │                   │
│                              ▼              ▼                   │
│                        決定論的パーサー   資格照合               │
│                         (正規表現)     (qualification.py)       │
│                              │                                  │
│                         失敗 + (cid)?                           │
│                              │                                  │
│                              ▼                                  │
│                    [層C] Claude Sonnet                           │
│                     (ADR-0012)                                  │
└─────────────────────────────────────────────────────────────────┘

┌─ 積算 (estimation) ─────────────────────────────────────────────┐
│                                                                  │
│  品目名の名寄せ                                                   │
│   ①完全一致 → 確定                                               │
│   ②正規化一致 → 確定                                              │
│   ③[層A] Embedding類似度 → 0.85以上: 確定 / 0.65-0.85: 層Cへ     │
│   ④[層C] Claude Haiku → LLMが候補から選択                        │
│   ⑤どれも不一致 → pending（人が手動マッチ）                       │
│                                                                  │
│  労務単価PDF構造化                                                 │
│   [層B] Qwen3-8B → テーブル構造をJSONに変換                       │
│                                                                  │
│  歩掛PDF構造化                                                    │
│   [層B] Qwen3-8B → 作業手順・数量をJSONに変換                     │
└──────────────────────────────────────────────────────────────────┘

┌─ 原価・工期分析 (ai) ───────────────────────────────────────────┐
│                                                                  │
│  コスト予測           [ML] LightGBM                              │
│   過去の完工実績から最終原価を統計的に予測                          │
│   テナントごとにモデル分離、最低5件の学習データが必要               │
│                                                                  │
│  コスト最適化提案     [層C] Claude Sonnet                         │
│   ML予測の数値 + 現場データを入力し、                              │
│   「なぜ超過するか」「どう対処すべきか」を自然言語で提案            │
│                                                                  │
│  工程提案             [層C] Claude Sonnet                         │
│  工程リスク分析       [層C] Claude Sonnet                         │
└──────────────────────────────────────────────────────────────────┘

┌─ 材料管理 (materials) ──────────────────────────────────────────┐
│                                                                  │
│  納品書OCR            [層B] Qwen3-8B                             │
│   納品書画像から品目・数量・単価を抽出                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## コスト管理

### 記録と予算制御

全ての AI 呼び出しは `AILog` モデルに記録される。

```python
# apps/ai/models.py
class AILog(TenantModel):
    task_type    # cost_forecast / cost_optimization / delivery_ocr / ...
    model_type   # claude-haiku / claude-sonnet / lgbm / custom
    prompt       # 入力プロンプト全文
    response     # 応答全文
    cost_usd     # このリクエストの費用（USD）
    tokens_in    # 入力トークン数
    tokens_out   # 出力トークン数
    latency_ms   # 応答時間（ミリ秒）
    status       # success / error / timeout
```

### 月間予算

`AI_MONTHLY_BUDGET_JPY=4000`（環境変数）で月間上限を設定。
上限到達時は `check_budget_and_notify()` が層C（Claude API）を停止する。
層A・層Bはローカル実行のため予算の影響を受けず、業務が止まる範囲を最小化する。

### コスト比較

| 処理 | Claude API | ローカル | 差 |
|------|-----------|---------|-----|
| 品目マッチング1件 | ¥0.18 (Haiku) | ≒¥0 (Embedding) | API不要に |
| PDF構造化1ページ | ¥0.18 (Haiku) | ¥0.04 (Qwen3-8B) | 4.5倍安い |
| コスト最適化提案1回 | ¥1.22 (Sonnet) | — | 品質のため据え置き |
| 文字化けPDF解読1件 | ¥10 (Sonnet) | — | 月数件。代替なし |

**年間の実質削減額は約5,000円。** コスト削減自体は小さいが、
「予算上限でAI機能が止まらなくなる」「一括マッチングを気兼ねなく回せる」ことが本当の価値。

---

## データフロー

### ML予測（LightGBM）のライフサイクル

```
完工済み現場の実績データ
        │
        ▼
  train_cost_model コマンド
        │
        ▼
  ml_models/{company_pk}_cost_model.joblib
        │
        ▼
  施工中の現場データを入力
        │
        ▼
  最終原価の予測値 + 特徴量重要度
        │
        ▼
  AILog に記録 ←→ AIFeedback（ユーザー評価 1-5）
```

詳細: [ADR-0005 LightGBM コスト予測](../saas/adr/ADR-0005-ml-cost-prediction.md)

### 品目名寄せのフロー

```
外部データ（発注機関の見積書、仕入先の見積書）
        │
        ▼
  品目名（表記ゆれあり）
        │
        ├─ ① 完全一致コード → 確定 (confidence=100)
        │
        ├─ ② 正規化一致     → 確定 (confidence=90)
        │
        ├─ ③ Embedding類似度
        │      0.85以上 → 確定 (confidence=85)
        │      0.65-0.85 → 候補をLLMへ
        │      0.65未満 → 不一致
        │
        ├─ ④ Claude Haiku → LLMが候補から選択 (confidence=70)
        │
        └─ ⑤ 不一致 → ItemAlias(status=pending)
                        → 人が画面で手動マッチ
```

詳細: [ADR-0008 積算アプリ設計](../saas/adr/ADR-0008-estimation-app.md)

### 入札公告の解析フロー

```
i-ppi 案件取得（Playwright）
        │
        ▼
  source_url → 公告PDFダウンロード
        │
        ▼
  pdfplumber でテキスト抽出
        │
        ├─ 読めた → 正規表現で章分割 → 工事概要 / 参加要件
        │                                    │
        │                                    ▼
        │                              資格マスタと照合
        │                              (qualification.py)
        │                                    │
        │                                    ▼
        │                              判定: 参加可 / 不可 / 要確認
        │
        └─ (cid) 化で読めない
                │
                ▼
          Claude Sonnet（画像として送信）
                │
                ▼
          工事概要 / 参加要件 / 等級 / 点数
```

詳細: [ADR-0011 入札資格・公告解析](../saas/adr/ADR-0011-bid-qualification-and-announcement.md) / [ADR-0012 文字化けPDF](../saas/adr/ADR-0012-garbled-announcement-llm-fallback.md)

---

## ADR 一覧

AI に関する設計判断を記録した ADR の全一覧。

| ADR | タイトル | 層 | 要約 |
|-----|---------|-----|------|
| [ADR-0005](../saas/adr/ADR-0005-ml-cost-prediction.md) | LightGBM コスト予測 | ML | 軽量・説明可能な ML。テナント別モデル、最低5件で学習 |
| [ADR-0006](../saas/adr/ADR-0006-claude-api-integration.md) | Claude API 統合 | 層C | Haiku/Sonnet 使い分け。全呼び出し AILog 記録 |
| [ADR-0008](../saas/adr/ADR-0008-estimation-app.md) | 積算アプリ設計 | 層A/C | 品目名寄せの段階的マッチング戦略 |
| [ADR-0010](../saas/adr/ADR-0010-ai-inference-tiering.md) | **AI推論の3層分割** | 全層 | **中核文書。** 実測データに基づく層分け・VRAM計算・損益分岐 |
| [ADR-0011](../saas/adr/ADR-0011-bid-qualification-and-announcement.md) | 入札資格・公告解析 | — | 決定論的パーサー。表記ゆれ吸収・等級の列挙vs下限の区別 |
| [ADR-0012](../saas/adr/ADR-0012-garbled-announcement-llm-fallback.md) | 文字化けPDF→LLM | 層C | (cid)化PDFのみClaude Sonnet。tesseract不採用の理由 |

> ADR-0010 が最も詳細（255行）で、AI設計の中核文書です。
> 実測値・損益分岐計算・VRAM配分・keep_alive戦略・Embedding精度検証が含まれています。

---

## 実行環境

```
desktop-rmsk0vg (Windows)
├── Docker Desktop
│   ├── web コンテナ   ← Django（AI呼び出し元）
│   ├── cron コンテナ  ← 定期バッチ
│   ├── db             ← PostgreSQL
│   └── redis          ← キャッシュ
│
├── Ollama (Windows ネイティブ)  ← 層A/B の推論
│   ├── bge-m3         ← Embedding (1.2GB)
│   └── qwen3:8b       ← 構造化/抽出 (5.2GB)
│   接続: host.docker.internal:11434
│
└── RTX 3060 12GB      ← GPU (推論時 155W / 74℃)
```

Ollama を WSL やコンテナ内ではなく Windows ネイティブで動かすのは、
GPU ドライバへの直接アクセスと安定性のため。
