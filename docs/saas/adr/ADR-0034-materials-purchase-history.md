# ADR-0034: 材料ごとの過去の取引を発注明細から引き、そこから発注し直せるようにする

## ステータス

採用（2026-09-10）

## コンテキスト

仕入先（発注先）は `masters` の画面、材料と発注書は `materials` の画面にあり、
「この材料をいつ・どこから・いくらで買ったか」を横断して見る画面が無かった。
発注書一覧は発注書単位、材料仕入先（`MaterialSupplier`）は標準単価の登録だけで、
実際の取引の単価の推移は発注書を1枚ずつ開かないと分からない。

プロダクトオーナーの決定（2026-09-10）:

- **材料ごとの過去の取引**を、材料・仕入先（加えて現場・期間）で検索できるようにする
- 検索は**材料・発注の画面の中**に置く（サイドバーに項目を増やさない）
- **過去の履歴から、同じ材料や同じ仕入先（下請け）に発注できる**ようにする

取引の記録は2か所にある。

| | `PurchaseOrderItem`（+ `PurchaseOrder`） | `ProcurementRecord` |
|---|---|---|
| できる時 | 発注書に明細を足した時 | 検収の時（`_create_procurement_records`） |
| 材料 | マスタ FK（空可）+ 自由入力の材料名 | マスタ FK 必須 |
| 持っている値 | 仕入先・現場・発注日・状態・数量・単位・単価 | 納品日・実リードタイム・納品数量 |
| 漏れるもの | 納品の情報 | 未検収の発注、自由入力の明細 |

## 決定

1. **発注明細を土台にする**。`materials.purchase_history.search_purchase_history(company, ...)` が
   発注明細を読み、材料ごとにまとめて返す。画面は `materials:purchase_history`（`/materials/history/`）
2. **検収済みの分は調達実績から納品日と実リードタイムを足す**。発注書 × 材料で突き合わせる。
   調達実績は1クエリで読み、Python で引き当てる（明細ごとに問い合わせない）
3. **下書きと取消は含めない**。下書きはまだ発注しておらず数量・単価が確定前、
   取消は取引が成立していない。どちらも混ぜると最安・最高単価が実在しない値でぶれる
4. **自由入力の明細（材料 FK なし）は、表記ゆれを潰した材料名でまとめる**。
   照合キーは見積取り込みと同じ `normalize_material_name`。マスタのある明細とは
   名前が同じでも混ぜない（マスタに引き当てるかは人が決めることなので）
5. 材料ごとの集計は 取引件数・単位ごとの合計数量・最新単価（最終発注日の単価）・
   最安/最高単価・仕入先数・最終発注日。金額は数量 × 単価を Decimal のまま出す
6. 1回に読む明細は発注日の新しい順に **500件まで**。超えたら画面で知らせ、絞り込みを促す。
   集計もこの範囲で行う
7. **材料・発注の中に置く**。材料・発注の画面群（`materials:*`）の上に
   「材料・発注 / 取引履歴」のタブを出し（ADR-0029 の SectionTabs）、材料・発注の一覧の上に
   「過去の取引を検索」の入力欄を置く。サイドバーの項目は増やさない
8. **取引の行から発注し直す**。
   - 「同じ材料で発注」: 発注書の作成画面を、その取引の仕入先・現場を選んだ状態で開く。
     保存すると、その明細（材料・材料名・数量・単位・単価・税率・工種）を新しい発注書にコピーする
     （`copy_item_to_order`）。数量・単価は作成後の発注書で直す
   - 「この仕入先に発注」: 仕入先だけを選んだ状態で作成画面を開く
   - 現場・発注日は作成画面で選び直せる。発注書には作成後に基本情報を編集する画面が無いため、
     下書きを先に作ってから直すのではなく、作成画面に初期値を入れる形にした
   - コピー元の明細は自社のものだけ受け付ける。他社の ID を渡されても黙って無視する

## 捨てた選択肢

- **調達実績（ProcurementRecord）だけを使う**: 検収前の発注と自由入力の明細が出ない。
  CSV 取り込みの発注書は明細がすべて自由入力なので、多くの取引が見えなくなる
- **見積（estimation.PurchaseRecord）を使う**: 卸の請求書を取り込んで見積単価を
  作るためのデータで、自社の発注書・現場と結びついていない
- **仕入先の画面（masters）に取引一覧を足す**: 求められているのは材料を起点に
  仕入先を比べる見方で、仕入先起点では同じ材料の単価を横に並べられない
- **集計を DB の集約（GROUP BY）で出す**: 自由入力の名前の表記ゆれは DB 上では
  潰せないので、まとめる単位が Python 側とずれる
- **サイドバーに「取引履歴」を独立項目で置く**: 材料・発注の中に置くよう指示があった
- **「同じ材料で発注」で下書きの発注書をその場で作る**: 作成後に現場・仕入先を変える画面が無く、
  別の現場向けに発注し直すときに作り直しになる

## 影響

- 追加: `apps/materials/purchase_history.py`（`search_purchase_history` / `copy_item_to_order`）、
  `views.purchase_history`、URL `materials:purchase_history`
- 変更: `views.po_create`（`?supplier=` / `?reorder_item=` の初期値とコピー）、`templates/materials/form.html`（コピー内容の表示）
- 変更: `apps/core/navigation.py`（`MATERIALS_TABS`）、`templates/materials/list.html`（検索欄）
- 追加: `templates/materials/purchase_history.html`、`static/css/style.css` に `.ph-*` / `.materials-history-search` / `.form-note`
- 追加: `tests/test_materials_purchase_history.py`（まとめ方・絞り込み・調達実績の付与・集計・越境・画面・タブ・発注し直し）
- モデル・マイグレーションなし
